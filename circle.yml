machine:
  java:
    version: openjdk8
    version: oraclejdk8
deployment:
  releases:
    branch: master
    commands:
      - mvn -B release:clean release:prepare
      - mvn -B -s settings.xml release:perform
